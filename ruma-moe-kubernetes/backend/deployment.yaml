apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: ruma-moe
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      imagePullPolicy: Always
      containers:
        - name: backend
          image: registry.walruslab.org/pub/ruma-moe-backend
          envFrom:
            - configMapRef:
                name: backend-config
